const STORE='dfm_react_pwa_v1';
function attachRemove(){const hero=document.querySelector('.detail-hero');if(!hero||hero.dataset.removeReady)return;const name=hero.querySelector('h2')?.textContent?.trim();let data;try{data=JSON.parse(localStorage.getItem(STORE)||'null')}catch{return}const index=data?.drones?.findIndex(item=>item.name===name)??-1;if(index<0)return;hero.dataset.removeReady='1';const button=document.createElement('button');button.type='button';button.className='danger-button';button.textContent='Smazat dron';button.onclick=()=>{if(confirm(`Opravdu odstranit dron „${name}“?`)){data.drones.splice(index,1);localStorage.setItem(STORE,JSON.stringify(data));location.reload()}};hero.querySelector('.detail-actions')?.appendChild(button)}
new MutationObserver(attachRemove).observe(document.documentElement,{childList:true,subtree:true});attachRemove();